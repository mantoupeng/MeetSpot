# 提交文案建议（供 Claude Code 使用）

要求：Conventional Commits、第一人称、不带 AI 署名。建议按逻辑拆分提交，而不是一个巨型 commit。

## 建议的提交拆分

1. `fix: 单个地址解析失败时继续基于其余地址推荐`

   包含 app/tool/meetspot_recommender.py 的部分成功改造、结果文本与 HTML 的跳过提示，以及 tests/test_geocode_failure.py。

2. `feat: 恢复 Agent 模式并补复杂度路由与降级测试`

   包含 api/index.py 的 agent_available 恢复、tests/test_agent_mode.py。

3. `fix: 高德 Web key 不再返回前端，health 按真实 key 判断`

   包含 /api/config/amap 改造与 /api/config/google_maps 说明、health 修正、tests/test_config_masking.py。

4. `feat: 预算档位软筛选生效并补 POI 回退与跨城测试`

   包含 _rank_places 价格筛选、tests/test_poi_fallback.py、tests/test_smart_city.py、tests/test_quota.py。

5. `chore: 补齐 Docker 一键启动与 CI 测试守护`

   包含 docker-compose.yml、.dockerignore、.env.example 补充、ci.yml 增加 pytest 与 ruff。

6. `docs: GOAI 2026 参赛材料与工程文档`

   包含 docs/ARCHITECTURE.md、docs/BENCHMARK.md、README 路线图、hackathon/ 材料包。

7. `chore: lint 清理`

   包含未使用导入与变量清理、.ruff.toml。

## PR 描述建议

标题：feat: GOAI 2026 参赛准备（稳定性、Agent 闭环与材料）

正文：

为 GOAI 2026 无界应用赛道准备：修复单个地址失败导致整个请求中止的问题；恢复 Agent 模式；高德 Web key 不再下发前端；预算档位筛选生效；CI 补跑 pytest 与 ruff；补齐 Docker 一键启动、架构文档、评测基准和参赛材料第一版。

验证：

- pytest tests/ -q：43 passed
- ruff check .：All checks passed
- docker compose up --build 实测 /health 与首页 200
- 本地端到端：部分失败、Agent 超时降级、预算筛选均实测通过

说明：线上部署环境变量 FREE_DAILY_LIMIT=2 与 LLM key 核对属于部署动作，未包含在本次提交。
